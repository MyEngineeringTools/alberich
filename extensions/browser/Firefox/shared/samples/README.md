# Demo-Tafeln (öffentlich, nicht produktiv)

| Datei | Version | Zweck |
|---|---|---|
| `demo-codebook.json` | 1 (Legacy Modern) | Alter Store-Pfad, Kreuzvektor „Hallo“ |
| `demo-codebook-v3.json` | 3 (Modern V3) | Standard-Demo in der Info: ALBV + Prüfgruppe |

Tafelwort V3: **CPTZ YYH** (ändert sich nicht mit `monthLabel` / `generatedAt`).

Tag 16 ist der dokumentierte Goldtag (`Hello` → `ALBVKCBDTESTMSGXXWSWIYDBURITGOYPOAXIECKLSASEE` bei Spruchschlüssel `LDNQ` und Message-ID `TESTMSGX`). Die Auto-ID in der UI ist zufällig — der Stempel `ALBV` und die Prüfgruppe müssen trotzdem erscheinen.
